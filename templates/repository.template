<?php

namespace {{ namespace }};

use Bektasyildiz\LaravelRepository\Repositories\BaseRepository;
use {{ useModel }} as Model;

class {{ class }} extends BaseRepository {
    
    public function __construct(Model $model)
    {
        parent::__construct($model);
    }
}